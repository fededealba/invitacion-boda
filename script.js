const translations = {
    es: {
        title: "Federico & Hélène",
        subtitle: "se van a casar 🎉",
        save_date_title: "Save the Date",
        nav_toggle_label: "Secciones",
        date: "Sábado 7 de marzo de 2026",
        place: "Tequisquiapan, Qro., Mexico",
        text: "Nos encantaría celebrar este día contigo.",
        details: "Detalles",
        details_title: "Detalles",
        rsvp: "Confirmar",
        home: "Inicio",
        wedding_details: "Detalles de la boda",
        couple_intro: "Boda de Federico De Alba Michel y Hélène Toulotte.",
        schedule: "Horario",
        friday_date: "Viernes 6 de marzo",
        saturday_date: "Sábado 7 de marzo",
        sunday_date: "Domingo 8 de marzo",
        icebreaker_title: "Evento de bienvenida",
        icebreaker_time: "18:00 · Norte B",
        ceremony_title: "Ceremonia",
        ceremony_time: "14:00 · Hacienda Grande",
        cocktail_title: "Cóctel",
        cocktail_time: "15:00 · Hacienda Grande",
        brunch_title: "Brunch",
        brunch_time: "13:00 · Nautha",
        location: "Lugares",
        location_subtitle: "Cómo llegar a cada evento",
        icebreaker_location_title: "Evento de bienvenida – Norte B",
        icebreaker_location_address: "Carretera Estatal 200-km 50, 76780 Tequisquiapan, Qro., Mexico",
        ceremony_location_title: "Ceremonia y cóctel – Hacienda Grande",
        ceremony_location_address: "Fernando de La Mora Magdaleno 82, 76750 Tequisquiapan, Qro., Mexico",
        brunch_location_title: "Brunch – Nautha",
        brunch_location_address: "Nautha, Centro, 76750 Tequisquiapan, Qro., Mexico",
        view_map: "Ver en Google Maps",
        view_website: "Sitio web del hotel",
        dress_code: "Código de vestimenta",
        dress_code_text: "Formal (traje y corbata, vestido elegante). Tacones altos no recomendados (césped y grava).",
        children_title: "Niños",
        children_text: "Niños bienvenidos.",
        food_title: "Comida",
        food_text: "Menú mexicano, opción vegetariana disponible.",
        allergies_text: "Alergias: escribir a ",
        transport_title: "Transporte",
        transport_text_1: "Estacionamiento disponible en Hacienda Grande.",
        transport_text_2: "También hay estacionamiento cerca de Nautha para el brunch.",
        practical_title: "Información práctica",
        accommodation_title: "Hospedaje recomendado",
        accommodation_text_1: "Hotel Quinta Los Vela.",
        accommodation_text_2: "Nautha 7, Centro, 76750 Tequisquiapan.",
        gifts_title: "Regalos",
        gifts_text_1: "Su presencia ya llena nuestro día de alegría (y sí, aceptamos totalmente el cliché 💛).",
        gifts_text_2: "Si aun así desean obsequiar algo, una contribución en efectivo será lo más sencillo.",
        gifts_text_3: "Pueden entregarla el día del evento, o dársela a los padres o al hermano de Federico antes de la boda si les resulta más cómodo.",
        contacts_title: "Contactos",
        contact_federico: "Federico: fede­dealba@gmail.com",
        contact_helene: "Hélène: toulotte.helene@gmail.com",
        contact_planner: "Wedding planner: jess@gmail.com",
        rsvp_title: "Confirmar asistencia",
        rsvp_text: "Por favor avísanos si puedes venir.",
        rsvp_message: "Envíanos un mensaje a:",
        or_whatsapp: "o escríbenos por WhatsApp 💌"
    },
    fr: {
        title: "Federico & Hélène",
        subtitle: "vont se marier 🎉",
        save_date_title: "Save the Date",
        nav_toggle_label: "Sections",
        date: "Samedi 7 mars 2026",
        place: "Tequisquiapan, Qro., Mexico",
        text: "Nous serions ravis de célébrer cette journée avec vous.",
        details: "Détails",
        details_title: "Détails",
        rsvp: "RSVP",
        home: "Accueil",
        wedding_details: "Détails du mariage",
        couple_intro: "Mariage de Federico De Alba Michel et Hélène Toulotte.",
        schedule: "Programme",
        friday_date: "Vendredi 6 mars",
        saturday_date: "Samedi 7 mars",
        sunday_date: "Dimanche 8 mars",
        icebreaker_title: "Soirée d'accueil",
        icebreaker_time: "18h00 · Norte B",
        ceremony_title: "Cérémonie",
        ceremony_time: "14h00 · Hacienda Grande",
        cocktail_title: "Cocktail",
        cocktail_time: "15h00 · Hacienda Grande",
        brunch_title: "Brunch",
        brunch_time: "13h00 · Nautha",
        location: "Lieux",
        location_subtitle: "Comment arriver à chaque événement",
        icebreaker_location_title: "Soirée d'accueil – Norte B",
        icebreaker_location_address: "Carretera Estatal 200-km 50, 76780 Tequisquiapan, Qro., Mexico",
        ceremony_location_title: "Cérémonie et cocktail – Hacienda Grande",
        ceremony_location_address: "Fernando de La Mora Magdaleno 82, 76750 Tequisquiapan, Qro., Mexico",
        brunch_location_title: "Brunch – Nautha",
        brunch_location_address: "Nautha, Centro, 76750 Tequisquiapan, Qro., Mexico",
        view_map: "Voir sur Google Maps",
        view_website: "Site web de l'hôtel",
        dress_code: "Code vestimentaire",
        dress_code_text: "Formelle : costume + cravate, robe habillée. Talons hauts déconseillés (pelouse et gravier).",
        children_title: "Enfants",
        children_text: "Les enfants sont les bienvenus.",
        food_title: "Nourriture",
        food_text: "Menu mexicain, options végétariennes disponibles.",
        allergies_text: "Allergies : écrire à ",
        transport_title: "Transport",
        transport_text_1: "Parking disponible à Hacienda Grande.",
        transport_text_2: "Parking aussi près de Nautha pour le brunch.",
        practical_title: "Informations pratiques",
        accommodation_title: "Hébergement recommandé",
        accommodation_text_1: "Hôtel Quinta Los Vela.",
        accommodation_text_2: "Nautha 7, Centro, 76750 Tequisquiapan.",
        gifts_title: "Cadeaux",
        gifts_text_1: "Votre présence suffit à remplir notre journée de bonheur (et oui, on assume complètement le cliché 💛).",
        gifts_text_2: "Si vous souhaitez malgré tout offrir quelque chose, une participation en espèces sera la plus simple.",
        gifts_text_3: "Vous pourrez l'apporter le jour du mariage, ou la remettre aux parents ou au frère de Federico avant l'événement si cela vous convient mieux.",
        contacts_title: "Contacts",
        contact_federico: "Federico : fede­dealba@gmail.com",
        contact_helene: "Hélène : toulotte.helene@gmail.com",
        contact_planner: "Wedding planner : jess@gmail.com",
        rsvp_title: "RSVP",
        rsvp_text: "Merci de nous dire si vous pouvez venir.",
        rsvp_message: "Envoyez-nous un message à :",
        or_whatsapp: "ou écrivez-nous sur WhatsApp 💌"
    }
};

function changeLanguage(lang) {
    localStorage.setItem('wedding-lang', lang);

    // Update document language attribute
    document.documentElement.lang = lang;

    // Update text content
    document.querySelectorAll('[data-i18n]').forEach(element => {
        const key = element.getAttribute('data-i18n');
        if (translations[lang] && translations[lang][key]) {
            element.textContent = translations[lang][key];
        }
    });

    // Update active state of buttons
    document.querySelectorAll('.lang-btn').forEach(btn => {
        btn.classList.toggle('active', btn.getAttribute('onclick').includes(lang));
    });
}

// Scroll-triggered animations
function handleScrollAnimations(prefersReducedMotion) {
    if (prefersReducedMotion) {
        document.querySelectorAll('section, .timeline-item, .location-card, .detail-card').forEach(element => {
            element.classList.add('visible');
        });
        return;
    }

    const sections = document.querySelectorAll('section');
    const timelineItems = document.querySelectorAll('.timeline-item');
    const locationCards = document.querySelectorAll('.location-card');
    const detailCards = document.querySelectorAll('.detail-card');

    const observerOptions = {
        threshold: 0.15,
        rootMargin: '0px 0px -100px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
            }
        });
    }, observerOptions);

    // Observe all sections
    sections.forEach(section => {
        observer.observe(section);
    });

    // Observe timeline items with staggered delay
    timelineItems.forEach((item, index) => {
        item.style.transitionDelay = `${index * 0.2}s`;
        observer.observe(item);
    });

    // Observe location cards with staggered delay
    locationCards.forEach((card, index) => {
        card.style.transitionDelay = `${index * 0.15}s`;
        observer.observe(card);
    });

    // Observe detail cards with staggered delay
    detailCards.forEach((card, index) => {
        card.style.transitionDelay = `${index * 0.1}s`;
        observer.observe(card);
    });
}

// Smooth scrolling for anchor links (if needed)
function initSmoothScroll() {
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });
}

// Initialize everything
document.addEventListener('DOMContentLoaded', () => {
    const prefersReducedMotion = window.matchMedia &&
        window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    // Initialize language
    const savedLang = localStorage.getItem('wedding-lang') || 'es';
    changeLanguage(savedLang);

    // Initialize scroll animations
    handleScrollAnimations(prefersReducedMotion);

    // Initialize smooth scroll
    initSmoothScroll();

    // Mobile navigation toggle
    const topNav = document.querySelector('.top-nav');
    const navToggle = document.querySelector('.nav-toggle');
    const smallScreenQuery = window.matchMedia
        ? window.matchMedia('(max-width: 768px)')
        : null;

    const updateNavForViewport = () => {
        if (!topNav || !navToggle || !smallScreenQuery) return;

        if (smallScreenQuery.matches) {
            topNav.classList.remove('is-open');
            navToggle.setAttribute('aria-hidden', 'false');
            navToggle.setAttribute('aria-expanded', 'false');
        } else {
            // Always show nav on larger screens
            topNav.classList.add('is-open');
            navToggle.setAttribute('aria-hidden', 'true');
            navToggle.setAttribute('aria-expanded', 'true');
        }
    };

    if (topNav && navToggle && smallScreenQuery) {
        updateNavForViewport();
        smallScreenQuery.addEventListener('change', updateNavForViewport);

        navToggle.addEventListener('click', () => {
            const isOpen = topNav.classList.toggle('is-open');
            navToggle.setAttribute('aria-expanded', String(isOpen));
        });

        topNav.querySelectorAll('.nav-item').forEach(link => {
            link.addEventListener('click', () => {
                if (smallScreenQuery.matches) {
                    topNav.classList.remove('is-open');
                    navToggle.setAttribute('aria-expanded', 'false');
                }
            });
        });
    }

    // Make hero section visible immediately
    const heroSection = document.querySelector('#hero');
    if (heroSection) {
        heroSection.classList.add('visible');
    }
});

// Advanced parallax and scroll effects
(() => {
    const supportsMatchMedia = typeof window.matchMedia === 'function';
    const prefersReducedMotion = supportsMatchMedia &&
        window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    if (prefersReducedMotion) {
        return;
    }

    const hero = document.querySelector('#hero');
    const saveTheDate = document.querySelector('#save-the-date');
    const schedule = document.querySelector('#schedule');
    const locations = document.querySelector('#locations');
    const details = document.querySelector('#details');
    const gifts = document.querySelector('#gifts');

    let ticking = false;

    function updateParallax() {
        const scrolled = window.pageYOffset;
        const windowHeight = window.innerHeight;

        // Hero parallax - moves slower and fades out
        if (hero && scrolled < windowHeight) {
            hero.style.transform = `translateY(${scrolled * 0.5}px)`;
            hero.style.opacity = 1 - (scrolled / windowHeight) * 0.8;
        }

        // Section parallax - different speeds for depth effect
        if (saveTheDate) {
            const rect = saveTheDate.getBoundingClientRect();
            if (rect.top < windowHeight && rect.bottom > 0) {
                const offset = (windowHeight - rect.top) * 0.15;
                saveTheDate.style.transform = `translateY(${offset}px)`;
            }
        }

        if (schedule) {
            const rect = schedule.getBoundingClientRect();
            if (rect.top < windowHeight && rect.bottom > 0) {
                const offset = (windowHeight - rect.top) * 0.1;
                schedule.style.transform = `translateY(${-offset}px)`;
            }
        }

        if (locations) {
            const rect = locations.getBoundingClientRect();
            if (rect.top < windowHeight && rect.bottom > 0) {
                const offset = (windowHeight - rect.top) * 0.12;
                locations.style.transform = `translateY(${offset}px)`;
            }
        }

        if (details) {
            const rect = details.getBoundingClientRect();
            if (rect.top < windowHeight && rect.bottom > 0) {
                const offset = (windowHeight - rect.top) * 0.08;
                details.style.transform = `translateY(${-offset}px)`;
            }
        }

        if (gifts) {
            const rect = gifts.getBoundingClientRect();
            if (rect.top < windowHeight && rect.bottom > 0) {
                const offset = (windowHeight - rect.top) * 0.1;
                gifts.style.transform = `translateY(${offset}px)`;
            }
        }

        ticking = false;
    }

    function requestTick() {
        if (!ticking) {
            requestAnimationFrame(updateParallax);
            ticking = true;
        }
    }

    window.addEventListener('scroll', requestTick, { passive: true });
})();

// Add 3D tilt effect on cards
(() => {
    const supportsMatchMedia = typeof window.matchMedia === 'function';
    const prefersReducedMotion = supportsMatchMedia &&
        window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    if (prefersReducedMotion) {
        return;
    }

    const cards = document.querySelectorAll('.location-card, .detail-card, .hero-card');

    cards.forEach(card => {
        card.addEventListener('mousemove', (e) => {
            const rect = card.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;

            const centerX = rect.width / 2;
            const centerY = rect.height / 2;

            const rotateX = (y - centerY) / 20;
            const rotateY = (centerX - x) / 20;

            card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`;
        });

        card.addEventListener('mouseleave', () => {
            card.style.transform = '';
        });
    });
})();

// Add smooth scroll reveal for text content
(() => {
    const supportsMatchMedia = typeof window.matchMedia === 'function';
    const prefersReducedMotion = supportsMatchMedia &&
        window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    if (prefersReducedMotion) {
        return;
    }

    const textElements = document.querySelectorAll('.text, p:not(.hero-date):not(.hero-place)');

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
            }
        });
    }, {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    });

    textElements.forEach(el => {
        if (!el.closest('.hero-card')) {
            el.style.opacity = '0';
            el.style.transform = 'translateY(20px)';
            el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
            observer.observe(el);
        }
    });
})();
